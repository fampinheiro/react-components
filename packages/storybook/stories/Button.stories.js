import React from 'react';
import { Button } from '@fampinheiro/react-components';

export default {
  title: 'Atoms/Button',
  component: Button,
};

const Template = (args) => <Button {...args} />;
export const Primary = Template.bind({});

